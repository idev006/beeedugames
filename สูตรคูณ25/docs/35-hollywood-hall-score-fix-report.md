# Hall of Fame Integrity & Hollywood Spotlight Report

วันที่: 2026-08-01  
สถานะ: Done

## ปัญหาและสาเหตุราก

1. ถ้วย/แท่นดูยืดและไม่ตรงแท่น เพราะ `podiums.png` ถูกใช้เป็น `background-image` ของกล่องผู้ชนะ แล้วบังคับให้ขยายตามขนาดกล่อง ทั้งที่ `background.png` มีแท่นสามแท่นอยู่แล้ว
2. Hall แสดง 1345 แม้หน้าผลลัพธ์ไม่มีดาว เพราะระบบเดิมใช้ normalized score ที่รวม accuracy, pace, mastery และ breadth โดย breadth ยังเพิ่มคะแนนได้แม้ไม่มีคำตอบถูก
3. ภาษาภาพของ Hall ยังไม่สื่อความเป็นช่วงเวลาประกาศรางวัลแบบงานเปิดตัว

## การแก้ไข

- ยกเลิกการวาด `podiums.png` ซ้ำและวาง avatar medal, ชื่อ และดาวตรงกับแท่นที่มีอยู่ในฉาก
- อันดับใช้ `result.stars` ซึ่งเป็นค่าที่เด็กเห็นและเข้าใจได้จากหน้าจอเกม
- รับรายการเข้าสู่ Hall เมื่อมีคำตอบถูกอย่างน้อยหนึ่งข้อ มีการซ่อมอย่างน้อยหนึ่งจุด และมีดาวมากกว่าศูนย์
- เปลี่ยน storage key เป็น `luminara.leaderboard.v2` เพื่อไม่ให้คะแนนสูตรเก่า เช่น 1345 ปะปนกับระบบดาว
- แสดงหน่วย `⭐ ... ดาว` ทั้งบนแท่นและในตารางอันดับ
- เพิ่มลำแสงเคลื่อนไหวสามทิศทาง แฟลชกล้อง winner glow และ floating animation
- รองรับ `prefers-reduced-motion` เพื่อหยุดเอฟเฟกต์เคลื่อนไหวสำหรับผู้ใช้ที่ไวต่อ motion

## Trophy correction

การแก้รอบแรกนำ `podiums.png` กลับมาใช้เป็นรางวัล แต่ asset นี้เป็นเสารางวัล ไม่ใช่ถ้วย จึงเกิดภาพแท่นซ้อนแท่น หลังทดสอบทั้ง SVG และ sprite ถ้วยร่วมกับเหรียญ พบว่าเกิด visual redundancy และลดความเด่นของอันดับ จึงตัดถ้วยออกจาก DOM ตาม design decision ล่าสุด และใช้เหรียญเกียรติยศเป็น visual language หลักเพียงระบบเดียว

- อันดับ 1: เหรียญทองขนาด 132px พร้อม aura ทองและวงแสงเด่นที่สุด
- อันดับ 2: เหรียญเงินขนาด 110px พร้อม aura เงิน–ฟ้า
- อันดับ 3: เหรียญทองแดงขนาด 92px พร้อม aura ส้ม–ทองแดง
- aura มี breathing pulse และ honor orbit คนละจังหวะ พร้อม reduced-motion fallback

## หลักฐานตรวจสอบ

- Automated tests: 29/29 ผ่าน
- ทดสอบจบเกมด้วย 0 ดาว: Hall ยังว่างและไม่สร้างรายการ
- ทดสอบตอบถูกหนึ่งรอบ: หน้าจอเกมเพิ่มเป็น 3 ดาว และ Hall แสดง `⭐ 3 ดาว`
- ตรวจที่ viewport 1280 × 720: เหรียญอันดับหนึ่งอยู่กึ่งกลางแท่น ไม่ยืดภาพ และเนื้อหาไม่ถูกตัด
- ตรวจถ้วย SVG บนแท่นกลาง: ไม่ซ้อนฐาน ไม่บิดสัดส่วน และ browser console ไม่มี error

## ผลต่อข้อมูลเดิม

ข้อมูล leaderboard รุ่น `v1` ไม่ถูกลบ แต่ระบบไม่อ่านมาแสดง เนื่องจากหน่วยคะแนนเดิมเปรียบเทียบกับดาวไม่ได้อย่างยุติธรรม ข้อมูลใหม่บันทึกแยกใน `v2` และพร้อมเปลี่ยน repository เป็น online implementation ภายหลังได้
